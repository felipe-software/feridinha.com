import { BasePageContainer } from "@/components/PageTransition"

export default function NotFound() {
    return (
        <BasePageContainer>
            <h1 style={{ margin: "auto", color: "var(--foreground)" }}>404</h1>
        </BasePageContainer>
    )
}
